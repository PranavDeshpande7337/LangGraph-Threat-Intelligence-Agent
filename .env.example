# Copy this file to .env and fill in your keys
# .env is gitignored — never commit real keys

ANTHROPIC_API_KEY=your_anthropic_key_here
VIRUSTOTAL_API_KEY=your_virustotal_key_here   # optional — falls back to mock
ABUSEIPDB_API_KEY=your_abuseipdb_key_here     # optional — falls back to mock
